{
  "name": "collab-canvas",
  "version": "1.0.0",
  "description": "Real-time collaborative drawing canvas (vanilla JS + hand-rolled WebSocket server, zero runtime dependencies)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT",
  "dependencies": {}
}
